interface:
  display_name: "EEG Provenance"
  short_description: "Auditable EEG preprocessing and harmonization"
  default_prompt: "Use $eeg-provenance in the smallest inspect, design, or execute mode that answers this EEG data request, and escalate tools or artifacts only when the endpoint requires them."
policy:
  allow_implicit_invocation: true
